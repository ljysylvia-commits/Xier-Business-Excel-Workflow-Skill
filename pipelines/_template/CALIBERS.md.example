# CALIBERS —— {{pipeline_id}} 固化口径清单（清洗动线版 · 脚手架）

> 用途：复用/数据更新时用户**唯一需要复核**的文件；执行侧单独加载，不必翻 9 章 plan。
> 来源：由 plan.md 第 7 章确认清单在注册时抽出生成。口径变更须同步本文件 + plan.md。
> 下面为通用占位示例，请按实际清洗规则替换（删除用不到的条目）。

## 一、口径决策（复用时逐条复核是否仍成立）

- [ ] 表头/数据起始位置：{{如：表头在第 N 行，数据从第 M 行开始}}
- [ ] 空值与占位符处理：{{如：字符串 "(NULL)"/"-"/空白 统一按空值跳过，不写入 CSV}}
- [ ] 日期识别：{{支持的日期格式，如 YYYY-MM-DD / YYYYMMDD / Excel 序列号}}
- [ ] 字段/标签统一命名：{{原始多种写法 → 统一值 的映射，如"今年/当年"→"当年值"}}
- [ ] 维度拆分规则：{{如：按指标名前缀拆出某维度列，无前缀=汇总}}
- [ ] 表/块合并规则：{{如：Sheet A + Sheet B 合并为一张长表，platform 区分来源}}
- [ ] 单位与精度：{{如：金额保留 2 位、比率存小数、统一 round(…,6)}}

## 二、Agent 推断项（用户需重点复核）

- [ ] {{Agent 从表头/样例推断、但未经用户确认的语义或规则}}

## 三、修正项与实现位置（清洗类关键：原表标签/口径有误时的纠正）

> 每条注明"原始→纠正"及**实现位置**（在哪个脚本的哪个常量/分支），便于日后维护定位。

- [ ] {{如：某指标原始标注为"同比"实为百分点差 → 纠正为"同比差值"；实现于 clean.py 的 XXX 常量/标志}}

## 四、结构依赖基线（数据结构漂移检测用）

> preflight 时将当前数据结构与此基线比对；缺失/改名 → 点名可能失效的口径 → 局部重共创。

- 依赖 Sheet/表：{{列出}}
- 依赖关键列/表头：{{列出}}
- 依赖维度取值：{{列出，如某维度列的合法取值集合}}
