# CALIBERS —— {{analysis_id}} 固化口径清单（数据分析套件版 · 脚手架）

> 用途：复用/数据更新时用户**唯一需要复核**的文件；执行侧单独加载。
> 来源：由 plan.md 第 7 章确认清单在注册时抽出生成。口径变更须同步本文件 + plan.md。
> 下面为通用占位示例，请按实际分析口径替换（删除用不到的条目）。

## 一、口径决策（复用时逐条复核是否仍成立）

- [ ] 聚合方式：{{总量 / 周期日均 / 累计 / 期末值 …}}
- [ ] 对比口径：{{同比/环比是读数据里现成字段，还是按同口径重算？重算更保口径一致}}
- [ ] 比例/占比分母：{{分子=？分母=？两者是否同周期同范围}}
- [ ] 周期定义：{{完整期与部分期怎么算、YTD/MTD 起止、跨年怎么处理}}
- [ ] 边界处理：{{单点缺失是否计入、基准为 0 或缺失时如何取值（如显示 #N/A）}}
- [ ] 单位与呈现：{{单位、小数位、比率差是否×100 成百分点、负值样式}}

## 二、Agent 推断项（用户需重点复核）

- [ ] {{Agent 从数据字典/表头推断的字段语义映射，请确认}}

## 三、假设项（用户当时未确定、按默认值先跑的口径）

> 交付时必须提醒；用户确认后移入第一区。

- [ ] {{假设内容 + 采用的默认值 + 登记日期}}

## 四、结构依赖基线（数据结构漂移检测用）

- 依赖来源表 / data_cleansing 套件：{{列出}}
- 依赖列与取值：{{列出取数所依赖的列名及关键取值}}
- （跨数据清洗套件分析）已确认的跨源口径映射：{{如：A 源的某取值 ≈ B 源的某取值（用户确认于 YYYY-MM-DD，
   附相关性/覆盖率等证据）——一经确认即固化于此，下次执行不重判}}
